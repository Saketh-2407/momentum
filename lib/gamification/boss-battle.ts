/**
 * A weekly boss battle is just a co-op-quest-shaped goal for one person —
 * "complete N tasks this week" — so progress math is computeQuestProgress
 * from lib/social/quest.ts reused, not reimplemented. This file only holds
 * the boss-battle-specific constants.
 */
export const WEEKLY_BOSS_BATTLE_TARGET = 15;
export const WEEKLY_BOSS_BATTLE_BONUS_XP = 200;
